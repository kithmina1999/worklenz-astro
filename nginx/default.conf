server {
    listen 80;
    listen [::]:80;
    server_name worklenz.ceydigital.dev;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    error_page 404 /404.html;
    location = /404.html {
        internal;
    }
}
